import 'dart:async';
import 'package:ble_task/ui/beacon_detection.dart';
import 'package:flutter/material.dart';

Future<void> main() async {
  WidgetsFlutterBinding.ensureInitialized();
  runApp(BeaconDetection());
  // runApp(MyApp());
}
